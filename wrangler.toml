name = "netravax-site"
pages_build_output_dir = "dist"
compatibility_date = "2026-07-01"

# Cloudflare Email Service binding used by functions/api/contact.js to send
# enquiry notifications directly -- no third-party email API or webhook.
# Requires the "from" domain (netravax.shubodaya.dev) to be onboarded to
# Email Sending first: npx wrangler email sending enable netravax.shubodaya.dev
[[send_email]]
name = "EMAIL"
