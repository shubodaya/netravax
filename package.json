{
  "name": "netravax-technologies-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "lint": "node scripts/validate-site.mjs",
    "test": "playwright test",
    "generate:icons": "node scripts/generate-icons.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "vite": "^7.1.0"
  },
  "dependencies": {
    "framer-motion": "^12.42.2",
    "gsap": "^3.15.0",
    "lenis": "^1.3.25"
  }
}
